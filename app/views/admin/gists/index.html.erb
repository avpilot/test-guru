<div class="row justify-content-center mt-4">  
  <div class="col col-md-8">
    <h1><%= t('.header') %></h1>
    <table class="table table-borderless">
      <tr>
        <th scope="col"><%= Question.model_name.human %></th>
        <th scope="col"> Gist url </th>
        <th scope="col"><%= User.model_name.human %></th>
      </tr>
      
      <%= render @gists %>
    </table>
  </div>
</div>
