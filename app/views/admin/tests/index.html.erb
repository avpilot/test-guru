<h1><%= t(:header, scope: "tests.index") %></h1>

<p>
  <%= link_to t(:new, item: Test.model_name.human), new_admin_test_path %>
</p>

<table>
  <tr>
    <th><%= Test.human_attribute_name(:title) %></th>
    <th><%= Test.human_attribute_name(:level) %></th>
    <th colspan="3"><%= t(".actions") %></th>
    <th><%= t(:question_count, scope: "tests.index") %></th>
  </tr>

  <%= render @tests%>
</table>
